{
  "name": "demo-uic-voice-widget",
  "version": "1.0.0",
  "description": "DEMO-UIC con Voice Widget integrado (ElevenLabs)",
  "type": "module",
  "main": "index.html",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:vercel": "vite build && node scripts/copy-build.js",
    "preview": "vite preview",
    "vercel-dev": "vercel dev",
    "deploy": "npm run build && vercel --prod",
    "vercel-build": "npm run build"
  },
  "keywords": [
    "voice",
    "widget",
    "elevenlabs",
    "uic",
    "ulinea"
  ],
  "author": "UIC",
  "license": "MIT",
  "dependencies": {
    "@elevenlabs/client": "^0.5.0"
  },
  "devDependencies": {
    "terser": "^5.44.0",
    "vercel": "^33.0.0",
    "vite": "^5.4.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
